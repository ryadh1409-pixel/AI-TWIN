# Backend is Firebase Functions. Set the API key as a function secret:
#   firebase functions:secrets:set OPENAI_API_KEY
#
# Optional (Cloud Run / function env): override transcription model
# OPENAI_TRANSCRIBE_MODEL=whisper-1
