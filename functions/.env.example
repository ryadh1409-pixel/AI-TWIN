# Emulator / local only (optional). Production uses Secret Manager:
#   firebase functions:secrets:set OPENAI_API_KEY
OPENAI_API_KEY=
# Optional override (default whisper-1). Some accounts support gpt-4o-mini-transcribe.
OPENAI_TRANSCRIBE_MODEL=whisper-1
